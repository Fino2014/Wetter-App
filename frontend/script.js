const button = document.getElementById('wetter-btn');
const output = document.getElementById('wetter-output');

button.addEventListener('click', () => {
    output.innerText = "Lade Wetterdaten... (Hier kommt bald eure KI!)";
});